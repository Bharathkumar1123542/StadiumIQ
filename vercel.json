{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/next" }
  ],
  "env": {
    "NEXT_PUBLIC_API_URL": "${RENDER_BACKEND_URL}"  
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "${RENDER_BACKEND_URL}/$1" }
  ]
}
